apiVersion: v1
kind: Service
metadata:
  name: {{ include "homework-6.fullname" . }}
  labels:
    {{- include "homework-6.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homework-6.selectorLabels" . | nindent 4 }}
